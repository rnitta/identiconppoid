name: identiconppoid
description: Generates Idention-like Images.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  scoped_model: ^1.0.0
  flutter_screenutil: ^0.4.2
  image: ^2.0.7
  path_provider: 0.5.0
  image_picker_saver: 0.1.0
  crypto: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
